# hardware-verification-system-frontend

React web app for HR/IT team. Login via Azure AD. Shows list of applicant PC verification results with status (PASS/FAIL/PENDING), submitted timestamp, and full specs. Filter by status/date. View detailed compliance breakdown per applicant. Export results to CSV. Mock data initially (TASKS B–H, U1–U7), real API integration later (TASK O).
